server:
  port: 8084
postgres:
  dsn: "postgres://devflow:devflow@192.168.1.2:32282/devflow?sslmode=disable"
  max_open_conns: 20
  max_idle_conns: 10
  conn_max_lifetime_minutes: 30
log:
  level: "info"
  format: "json"   # json | console
otel:
  endpoint: "127.0.0.1:4317"
  service_name: "verify-service"
