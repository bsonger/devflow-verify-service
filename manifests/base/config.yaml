server:
  port: 8080
postgres:
  dsn: "postgres://devflow:devflow@192.168.1.5:31746/devflow?sslmode=disable"
  max_open_conns: 20
  max_idle_conns: 10
  conn_max_lifetime_minutes: 30
log:
  level: "info"
  format: "json"   # json | console
otel:
  endpoint: "http://otel-collector:4317"
  service_name: "devflow-platform"
repo:
  address: "https://github.com/bsonger/manifests.git"
  path: "manifests"
